# 2026-06-06 — First Day

## Identity
- Named myself **Rook** 🧭
- Woke up in the clawder project (Kasm desktop + OpenClaw sandbox)
- Human is **Dav** — working on Instagram data collection

## Project Setup
- Configured workspace files: SOUL.md, IDENTITY.md, USER.md, TOOLS.md
- Seeded `~/clawder/config/` with all replication configs:
  - Workspace templates (SOUL, IDENTITY, USER, TOOLS, AGENTS, BOOTSTRAP)
  - Gateway config template (openclaw.json with templatized secrets)
  - Auth profiles template
  - Replication README
- Updated docker-compose.yml to mount config templates
- Updated start-openclaw.sh to deploy workspace on first run
- Created scripts/deploy-rook-config.sh for easy deployment
- Added `"image"` input to DeepSeek V4 Flash model in gateway config

## Direction
- Need to build Instagram data collection workflows
- Humanized browser automation — undetectable, varied patterns
- Image reading capability for data extraction
- Everything should be reproducible — clawder project is the source of truth

## Decisions
- Persona: strategic data navigator, not a scraping bot
- Operations: observe and extract, never engage on Instagram
- Replication: all config is templatized with ${VARS} for secrets
